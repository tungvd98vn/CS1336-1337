
#include <iostream>
#include <stdio.h>
#include <stdlib.h>
#include <string>
#include <cstring>
using namespace std;

int main(){
    int i;
    string input;
    exit
    do {
    getline(cin, input);
    const char *c = input.c_str();
    i = system(c);
    } while (input != "exit" && input != "quit" && input != "bye" && input != "done");
}
